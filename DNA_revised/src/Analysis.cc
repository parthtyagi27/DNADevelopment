#include "../includes/Analysis.h"

Analysis::Analysis(std::vector<Person> people, DNA sequence) {
    this->people = people;
    this->input_sequence = sequence;
}

std::vector<int> Analysis::find_count() {

}